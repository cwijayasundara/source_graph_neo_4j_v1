# financegraph — Financial Services Context Graph

[![Neo4j Labs](https://img.shields.io/badge/Neo4j_Labs-blue?logo=neo4j)](https://neo4j.com/labs/)

Investment management, trading, compliance, and risk assessment

> AI-powered Financial Intelligence

Built with [create-context-graph](https://github.com/neo4j-labs/create-context-graph) · Agent Framework: **LangGraph**

> **Neo4j Labs Project** — Generated by create-context-graph, a [Neo4j Labs](https://neo4j.com/labs/) project. Maintained by Neo4j staff and the community, but not officially supported.

## Quick Start

```bash
# Install dependencies
make install

# Start Neo4j (Docker)
make docker-up

# Seed sample data
make seed

# Start the application
make start
```

- **Backend:** http://localhost:8000
- **Frontend:** http://localhost:3000
- **Neo4j Browser:** http://localhost:7474


## Architecture

```
financegraph/
├── backend/          FastAPI + LangGraph
├── frontend/         Next.js + Chakra UI v3 + NVL
├── cypher/           Neo4j schema & GDS projections
├── data/             Domain ontology & generated documents
├── .env              Configuration (not tracked in git)
├── .env.example      Configuration template
└── Makefile          Development commands
```

## Domain: Financial Services

### Base POLE+O Entities

These entity types are shared across all domains using the Person-Organization-Location-Event-Object model:

- **Person** — PERSON
- **Organization** — ORGANIZATION
- **Location** — LOCATION
- **Event** — EVENT
- **Object** — OBJECT

### Domain-Specific Entities
- **Account** — OBJECT
- **Transaction** — EVENT
- **Decision** — EVENT
- **Policy** — OBJECT
- **Security** — OBJECT

### Demo Scenarios

#### Portfolio Analysis
- "Show me a summary of all client accounts and their current balances"
- "What are the recent transactions for the Smith family trust?"
- "Which portfolios have the highest risk exposure?"

#### Compliance & Risk
- "Are there any accounts flagged for compliance review?"
- "Show me all trades that exceeded position limits this quarter"
- "What policies apply to international wire transfers?"

#### Decision Intelligence
- "What was the reasoning behind the decision to sell AAPL last week?"
- "Find similar past decisions to a proposed large bond allocation"
- "Show me the causal chain for the Smith portfolio rebalance"

## Agent Tools

The AI agent has access to the following domain-specific tools:

- **search_customer** — Search for clients, advisors, or other people by name or role
- **get_customer_decisions** — Get all decisions related to a specific client
- **find_similar_decisions** — Find decisions similar to a given decision using vector similarity
- **get_causal_chain** — Trace the causal chain of events from a decision
- **detect_fraud_patterns** — Detect unusual transaction patterns that may indicate fraud
- **list_accounts** — List Account records with optional limit
- **get_account_by_id** — Get a specific Account by ID with all connections
- **execute_cypher** — Run ad-hoc Cypher queries
- **get_schema** — View the graph schema

## Agent Framework: LangGraph

This project uses [LangGraph](https://langchain-ai.github.io/langgraph/) with `@tool` decorators and `create_react_agent()`. The agent is a compiled graph that manages tool execution state. Supports full streaming via `graph.astream_events()`.

Requires `ANTHROPIC_API_KEY` in `.env` (uses `langchain-anthropic`).

See `backend/app/agent.py` for the full implementation.

## MCP Server (Claude Desktop Integration)

This project includes an MCP server configuration that lets Claude Desktop query
the same knowledge graph your web application uses.

```bash
# Start the MCP server
make mcp-server
```

See `mcp/README.md` for detailed setup instructions including how to configure
Claude Desktop and the list of available tools.

## Environment Variables

Copy `.env.example` to `.env` and fill in your values:

```bash
cp .env.example .env
```

| Variable | Description |
|----------|-------------|
| `NEO4J_URI` | Neo4j connection URI |
| `NEO4J_USERNAME` | Neo4j username |
| `NEO4J_PASSWORD` | Neo4j password |
| `ANTHROPIC_API_KEY` | Anthropic API key for the AI agent |
| `OPENAI_API_KEY` | OpenAI API key (for embeddings) |
| `SESSION_STRATEGY` | Memory session strategy (per_conversation, per_day, persistent) |
| `BACKEND_PORT` | Backend port (default: 8000) |
| `FRONTEND_PORT` | Frontend port (default: 3000) |

## Troubleshooting

### Neo4j Connection Errors

**"Neo4j is unavailable" on all endpoints**
- Verify your `.env` file has correct `NEO4J_URI`, `NEO4J_USERNAME`, and `NEO4J_PASSWORD`
- Run `make test-connection` to validate credentials
- Run `make docker-up` first, then wait a few seconds for Neo4j to start

**Extra variables in `.env`**
- The backend accepts extra environment variables, so standard Neo4j Aura `.env` exports (with `NEO4J_DATABASE`, `AURA_INSTANCEID`, etc.) work without editing

### Backend Issues

**Port already in use**
- Change `BACKEND_PORT` in `.env` (default: 8000)
- Kill existing process: `lsof -ti:8000 | xargs kill`

**API key not set**
- Set `ANTHROPIC_API_KEY` or `OPENAI_API_KEY` in `.env` depending on your agent framework

### Frontend Issues

**Build errors after `npm install`**
- Delete `node_modules` and `package-lock.json`, then run `npm install` again
- Ensure Node.js 18+ is installed
